language: cpp
os:
  - linux
dist: bionic
compiler:
  - gcc
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-10
      - g++-10
before_install:
  - export CXX=g++-10
script:
  - cmake .
  - make
  - ./tests
